# 更新日志

本文记录多人格管理插件的主要变更。

## v3.5.3 - 2026-06-15

### 新增

- 管理界面通过 Smart ImageChat Hub 自身候选入口读取原图库。
- 原图库以只读方式展示图片、文件名和最终检索标签。
- 支持预览原图库图片，并将整个原图库复制为独立、可编辑的人格图库。
- 复制过程按图片内容哈希去重，不修改 Smart ImageChat Hub 原索引。

## v3.5.2 - 2026-06-14

### 新增

- 增加 Smart ImageChat Hub 人格图库适配。
- 支持按人格隔离 Smart ImageChat Hub 发图候选、主动表情和斗图候选。
- 新增智能图片人格图库管理：创建、重命名、复制、删除、切换、图片上传和 ZIP 上传。
- 支持缓冲池图片多选复制、移动、删除和 ZIP 备份。
- 支持每个图库独立维护图片标签，复制图库时生成独立标签副本。
- 支持读取 Smart ImageChat Hub 原有公用特征标签，并与本插件额外公用标签合并为标签编辑器快捷选项。
- 支持把当前图片标签多选套用到包含相同 SHA-256 图片的其他图库。
- 支持上传图片后生成基础标签，并可调用 Smart ImageChat Hub 视觉能力进行智能打标。

### 修复

- 修复 Smart ImageChat Hub 隔离开关保存后被错误还原的问题。
- 修复智能图片人格图库管理入口和相关按钮无响应的问题。
- 修复图库下拉中的图片数量显示与重复命名后缀混淆的问题。
- 优化公用特征标签勾选项的显示尺寸。

### 调整

- Meme Manager 兼容卡片和文档标注为“暂停维护”。
- Meme Manager 当前仅保留既有适配与安全回退，不再继续扩展新能力。

## v3.4.13 - 2026-06-14

### 修复

- 修复旧配置导入时私聊列表混入群聊会话的问题。
- 修复 AstrBot 统一会话 ID 中的 `FriendMessage` 和 `GroupMessage` 被直接写入策略的问题。
- 旧配置中的顶层私聊用户、群聊、人格计划目标和导入忽略项会一次性迁移为 `平台:ID` 格式。
- 群聊规则支持优先匹配 `平台:群ID`，再回退到裸群号规则。
- 多机器人场景下导入会话使用 `default:<用户ID>` 这类平台作用域 ID，避免不同机器人之间互相覆盖。

## v3.4.12 - 2026-06-13

### 修复

- 修复会话导入分类，避免把群聊事件作为私聊用户导入。
- 删除过的导入项会进入忽略列表，更新或重新扫描时不会自动导回来。
- 手动重新添加用户或群聊后，会从导入忽略列表中移除。
- 适配器在 Proactive Chat、Private Companion 和 Life Scheduler 等路径中补充平台作用域别名匹配。

## v3.4.11 - 2026-06-13

### 调整

- 查询类命令默认改为仅管理员或授权用户可用。
- 新增查询命令权限配置，避免所有用户默认都能查看策略信息。

## v3.4.10 - 2026-06-13

### 新增

- 新增策略匹配日志开关，便于排查人格命中、插件权限和会话别名匹配问题。

## v3.4.9 - 2026-06-12

### 首次发布

- 支持私聊用户、群聊和群成员分别配置人格。
- 支持固定人格、自动人格和独立回复模型。
- 支持 AstrBot 对话记忆隔离与 LivingMemory 人格记忆隔离。
- 支持插件权限控制、用户封禁、群成员访问名单和群策略管理员。
- 支持人格计划，按每天、每周或随机间隔切换人格。
- 支持导入已有 AstrBot 私聊和群聊会话。
- 支持 Private Companion、LivingMemory、Proactive Chat、Life Scheduler、Meme Manager、Gitee AI Image 等兼容适配。
- 提供基于 AstrBot Plugin Pages 的 WebUI 管理界面。
